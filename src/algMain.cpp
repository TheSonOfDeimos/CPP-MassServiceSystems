#include <iostream>

#include "timeMeneger.h"
#include "buffer.h"



int main(int argc, const char * argv[]) {
    
    SourcePool sp(100000);
    /*
    Buffer buff;
    DevicePool dp(100000);
    
    TimeMeneger tm(&sp, &buff, &dp);
    
    for (int i = 0; i < 1000; ++i)
    {
        tm.loopIteration();
        std::cout << "\n";
    }
    */
    
    return 0;
}
